# Self Hosting Notes

This slice is designed for self-hosted runtime with config-gated live integrations.

- Keep `DRY_RUN_DEFAULT=true` until credentials are validated.
- Enable each integration independently via env flags.
