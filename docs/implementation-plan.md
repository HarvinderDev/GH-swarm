# Implementation Plan

## Milestone 1 (completed)
- Create workspace directories and package manifests.
- Configure turbo task graph and root scripts.
- Add strict TypeScript base config and per-workspace tsconfig.
- Add baseline ESLint, Prettier, and Vitest configuration.
- Add minimal source + test files for every workspace.

## Next milestones
- Add actual service stacks (Next.js/Fastify/worker runtime) and dependency graph.
- Add CI and dockerized local runtime.
