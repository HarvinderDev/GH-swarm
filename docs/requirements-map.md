# Requirements Map

## Requested in this session
- Initialize a pnpm + turbo monorepo with specified apps and packages.
- Add root scripts: install, dev, build, lint, typecheck, test, smoke, seed/demo.
- Add baseline lint/format/type/test config.
- Ensure strict TypeScript compilation for all workspaces.

## Alignment to PLAN.md phase
- This work covers **Phase 1 — repo bootstrap** foundations.
