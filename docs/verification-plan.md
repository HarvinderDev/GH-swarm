# Verification Plan

For this provider milestone:

1. Install dependencies.
2. Run workspace typecheck.
3. Run workspace build.
4. Run workspace tests (currently smoke/no-test harness).

Live validation remaining:

- Run against an installed/authenticated `codex` CLI.
- Verify JSON session listing/creation compatibility with local CLI version.
