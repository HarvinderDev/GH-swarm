# Local development

1. `corepack enable`
2. `pnpm install`
3. `cp .env.example .env`
4. `pnpm dev`

Services:
- web: http://localhost:3000
- api: http://localhost:4000/health
- worker: logs in terminal
