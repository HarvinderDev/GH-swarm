{
  "name": "@codex-swarm/api",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @codex-swarm/domain build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @codex-swarm/domain build && tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@codex-swarm/domain": "workspace:*",
    "fastify": "^5.3.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
